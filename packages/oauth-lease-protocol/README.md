# oauth-lease-protocol

Built as part of this source workspace; not published independently to npm.

Defines short-lived access-token leases between the host and tool subprocesses. The host manages refresh tokens and never passes them to tool subprocesses.
