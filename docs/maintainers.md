# Maintenance scope

For v0.1.0 preserve the upstream behavior and Pi. Make only identity/theme/distribution changes and focused fixes required by their regressions. Do not add a new execution owner, memory service, feature set or speculative architecture.

Keep the source inventory and generated TypeScript paths current, run the full qualification suite, preserve upstream attribution, and review every new brand-scan exception. See `releasing.md` for the artifact gate.
